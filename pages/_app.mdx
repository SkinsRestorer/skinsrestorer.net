import '../style.css'
import PlausibleProvider from "next-plausible";

export default function App({ Component, pageProps }) {
    return (
        <PlausibleProvider trackOutboundLinks trackFileDownloads domain="skinsrestorer.net">
            <Component {...pageProps} />
        </PlausibleProvider>
    )
}
